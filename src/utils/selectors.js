// User authentications
export const userAuthSelector = (state) => state.auth.user;
export const userTokenSelector = (state) => state.auth.token;
export const userIsLoggedSelector = (state) => state.auth.isLogged;
